[project]
name = "notifications-server"
version = "0.1.0"
description = "MCP server for sending hardware notifications over serial."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=2.1.1",
    "pyserial>=3.5",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
